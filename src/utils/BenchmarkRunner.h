#pragma once

namespace distance_benchmark {

/// Instantiates the processor, runs all configured scenarios, prints CSV  to stdout.
void runAndPrintCsvToStdout();

} // namespace distance_benchmark
